#include <iostream>
#include <vector>


#include "../vector/vector.hpp"



int main(){
    ft::vector<int>::iterator v;
    // std::vector<int> v1;
    // std::vector<int> v2;

    // v1.push_back(1);
    // v1.push_back(2);

    // v2.push_back(0);
    // v2.push_back(1);
    // v2.push_back(2);

    // std::cout << v1.size() << " " << v2.size() << std::endl;
    // std::cout << (v1 < v2) << std::endl;
    // std::cout << (v1 > v2) << std::endl;

    return 0;
}